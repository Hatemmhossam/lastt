<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="/css/edit.css">
    <style>


        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        
        .edit-popup {
            width: 50%;
            margin: 50px auto;
            background-color: white;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
        }
        
        h2 {
            text-align: center;
        }
        
        label {
            display: block;
            margin-top: 10px;
        }
        
        input, select, textarea, button {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
        }
        
        button {
            background-color: #5cb85c;
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
            margin-top: 10px;
        }
        
        button:hover {
            background-color: #4cae4c;
        }
        
        a {
            display: block;
            text-align: center;
            margin-top: 10px;
            color: #5cb85c;
        }
        
        a:hover {
            text-decoration: none;
            color: #4cae4c;
        }
        
    </style>
</head>
<body>
    <div class="edit-popup">
        <h2>Edit User</h2>
        <form action="/users/<%= user._id %>/edit" method="POST">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= user.name %>" required>
            
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" value="<%= user.email %>" required>

            <label for="password">Password:</label>
            <input type="password" id="password" name="password" value="<%= user.password %>" required>
            
            <label for="phone">Phone:</label>
            <input type="tel" id="phone" name="phone" pattern="[0-9]+" title="Please enter only numeric characters (0-9)" value="<%= user.phone %>" required>
            
            <label for="location">Location:</label>
            <select id="location" name="location" required>
                <option value="" selected disabled>Select your location</option>
                <option value="Maadi" <%= user.location === 'Maadi' ? 'selected' : '' %>>Maadi</option>
                <option value="New Cairo" <%= user.location === 'New Cairo' ? 'selected' : '' %>>New Cairo</option>
                <option value="Nasr city" <%= user.location === 'Nasr city' ? 'selected' : '' %>>Nasr city</option>
                <option value="Shrouk" <%= user.location === 'Shrouk' ? 'selected' : '' %>>Shrouk</option>
                <option value="Sheikh Zayed" <%= user.location === 'Sheikh Zayed' ? 'selected' : '' %>>Sheikh Zayed</option>
                <option value="6th of October" <%= user.location === '6th of October' ? 'selected' : '' %>>6th of October</option>
            </select>
            
            <button type="submit">Save Changes</button>
        </form>
        <a href="/viewRestraunts">Cancel</a>
    </div>
</body>
</html>
